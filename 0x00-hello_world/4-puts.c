#include <stdio.h>
/**
 * main - using puts function to print text on screen
 * Return: program ends with the value 0
 */
int main(void)
{
	char myStr[] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);
	return (0);
}
