#include <stdio.h>

/**
 * Using puts inside 
 * main to print 
 * text on screen 
 */
int main(void)
{
	char myStr[] = "\"Programming is like building a multilingual puzzle";

	puts(myStr);

	return (0);
}
