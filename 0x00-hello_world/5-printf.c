#include <stdio.h>
/**
 * main - using printf to display text on screen
 * Return: returns value 0
*/
int main(void)
{
	char myStr[] = "with proper grammar, but the outcome is a piece of art,";	
	printf(myStr);
	return (0);
}
