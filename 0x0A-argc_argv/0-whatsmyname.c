#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the programs name
 * @argc: int
 * @argv: array of strings
 * Return: 0
 */
int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
